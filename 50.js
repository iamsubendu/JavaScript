// this keyword

// In JavaScript, this is a keyword that refers to the current
// execution context of a function.
// Behaves little differenlty in JS compared to other languages.
// It also has some differences in strict & non-strict method

//  Global Context
// In the global context, this refers to the global object,
// which is usually the window object in a browser or the
// global object in a Node.js environment.

console.log(this);

// will represent global object
